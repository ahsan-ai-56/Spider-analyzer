{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm install --no-frozen-lockfile && cd lib/db && pnpm run build && cd ../../artifacts/api-server && pnpm run build",
  "builds": [
    {
      "src": "artifacts/api-server/api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "artifacts/api-server/api/index.js"
    }
  ]
}
